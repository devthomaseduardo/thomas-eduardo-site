services:
  portfolio:
    build: .
    container_name: portfolio-professional
    restart: unless-stopped
    ports:
      - "3004:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

